# Webframework

```python
# clone this
git clone https://github.com/Chara98/Webframework.git

# install libs
pip install -r requirements.txt

# Run this
waitress-serve --listen=0.0.0.0:8000 app:app

# app.py is an example for using this framework
```